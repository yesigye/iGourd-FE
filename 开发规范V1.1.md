# Vue 前端开发规范（v1.0）

> 适用：Vue 3 + TypeScript + Vite/Webpack + Pinia + Vue Router
> 目标：统一风格、降低协作成本、避免跨平台大小写问题、提升可维护性与可测试性
> 标准：全部使用 Typescript，不使用 Javascript

---

## 目录

* [1. JS/TS 命名规范](#1-jsts-命名规范)
* [2. 文件与目录命名](#2-文件与目录命名)
* [3. i18n 规范](#3-i18n-规范)
* [4. 权限点规范](#4-权限点规范)
* [5. URL/路由规范](#5-url路由规范)
* [6. 其他工程规范](#6-其他工程规范)
* [附录 A：ESLint/Prettier/Commitlint](#附录-aeslintprettiercommitlint)
* [附录 B：Git/CI 与大小写重命名](#附录-bgitci-与大小写重命名)
* [附录 C：常用代码片段](#附录-c常用代码片段)
* [附录 D：落地清单](#附录-d落地清单)

---

## 1) JS/TS 命名规范

**通用**

* 变量/函数：`camelCase` —— `userName`, `isLoading`, `fetchUserList`
* 类型/接口/类/枚举：`PascalCase` —— `User`, `IUser`, `UserService`, `UserRoleEnum`
* 常量：`SCREAMING_SNAKE_CASE` —— `API_BASE_URL`, `MAX_PAGE_SIZE`
* 布尔：`is/has/can/should` 前缀 —— `isAdmin`, `hasPermission`, `canEdit`
* 泛型参数：`T | U` 或具名 —— `ResultT`

**Vue 专用**

* 组合式函数（composable）：`useXxx` —— `useUserQuery`, `usePermission`
* Store：`useXxxStore` + `id: 'xxx'` —— `useUserStore` / `id: 'user'`
* Emits 事件：`kebab-case` —— `update:model-value`, `row-click`
* Props：脚本 `camelCase`、模板 `kebab-case` —— `pageSize` ⇔ `:page-size="20"`

**函数与类型**

* 异步函数以 `Async` 结尾：`loadUsersAsync`
* 导出函数/公共 API 必须显式返回类型
* 禁止裸 `any`；必要时用 `unknown` + 类型收缩（类型守卫）

---

## 2) 文件与目录命名

> **强制**：**组件 SFC 文件名一律 `kebab-case.vue`**（仅小写字母/数字/连字符）
> **特例**：**语言** 一律使用语言标准语言名-地区命名，`zh-CN`、`en-US`
> 理由：规避 Windows/macOS 与 Linux 文件系统大小写差异导致的跨平台编译问题。

**目录结构（建议）**

```
src/              # 应用壳(路由/布局/全局样式)
  features/
    users/
      pages/
      components/
      apis/
      stores/
      hooks/
      types/
      locales/
          en-US/
            common.json  # 模块加载时加载
            account.json # 页面加载时加载
          zh-CN/
            common.json  # 模块加载时加载
            account.json # 页面加载时加载
      utils/
      index.ts         # barrel
  assets/
  styles/
```

**命名规则**

* 目录：`kebab-case` —— `user-center`, `order-detail`
* 组件文件：`kebab-case.vue` —— `user-table.vue`, `order-detail-dialog.vue`
* 组件“内部名”：`PascalCase`（推荐）
  `defineOptions({ name: 'UserTable' })`
* 关联文件：`user-table.spec.ts`, `user-table.stories.ts`, `user-table.module.scss`
* 其他 TS/工具：`kebab-case.ts` —— `use-permission.ts`, `user-api.ts`
* Barrel：`index.ts`（仅导出、无业务逻辑）

**导入路径**

```ts
import UserTable from '@@/features/users/components'  // 与磁盘名完全一致
```

---

## 3) i18n 规范

**结构与加载**

```
src/locales/
  en-US/
    common.json
  zh-CN/
    common.json
```

* 首屏仅加载 `common`；特性路由懒加载对应包（节省体积）


**Key 命名**

* 规则：`<feature>.<scope>.<item>`（小写点分层）
  例：`users.table.name`, `orders.form.submit`
* 禁止用整句做 key；禁止在代码中拼接 key
* i18n key 遵循 `kebab-case` —— `user-name`, `title`
* i18n json 遵循模块化命名空间 `common.search`、`inventory.count.exchange-rate`

**占位/复数（ICU）**

```json
{
  "users": {
    "list": {
      "title": "Users",
      "summary": "{count, plural, =0 {No users} one {# user} other {# users}}",
      "greet": "Hello, {name}!"
    }
  }
}
```

```ts
t('users.list.summary', { count })
```

**文案与质量**

* 中英文混排：`中文 English 混排`
* 标点：中文用全角；时间/货币格式使用 `Intl.*`
* 未命中 key：开发模式必须报警（toast/console）

---

## 4) 权限点规范

**模型**

* 推荐 **RBAC + 细粒度权限点**
* 命名：`<domain>.<resource>.<action>`
  例：`users.user.read`, `users.user.create`, `orders.order.export`
* 保留应急：`admin.all`（仅紧急场景）

**路径**

* 路径：`kebab-case`，无尾斜杠 —— `/user-center`, `/order-detail/:orderId`

那这3个部分，都统一一下，统一使用： 中划线。

* 路径：`kebab-case`，无尾斜杠 —— `/user-center`, `/order-detail/order-id`
权限key：`users.user.read`, `users.user.create`, `orders.order.export-all`

**前端接入**

* 路由 Meta：

```ts
{
  path: '/users',
  name: 'Users',
  component: () => import('./pages/users.page.vue'),
  meta: { auth: { allOf: ['users.user.read'] } }
}
```

* 指令：`v-permission="'users.user.create'"`（默认隐藏；可配置禁用 + tooltip）
* Hook：`const { can, anyOf, allOf } = usePermission()`

**后端为准**

* UI 控显隐仅作“提示/引导”，实际鉴权由服务端判定
* 必须处理 `403`：统一提示 & 上报

---

## 5) URL/路由规范

**路径**

* 路径：`kebab-case`，无尾斜杠 —— `/user-center`, `/order-detail/:orderId`
* 动态段语义化：`:userId`, `:orderId`
* 路由名：`PascalCase` —— `UserList`, `OrderDetail`
* 如需多语言：可选前缀 `/:locale(en-US|zh-CN)?/...`

**查询参数**

* 键 `camelCase`；值需 `encodeURIComponent`
* 布尔统一一种：`true/false` 或 `1/0`

**构建工具**

```ts
export function buildUrl(
  path: string,
  query?: Record<string, string | number | boolean | undefined>,
) {
  if (!query) return path;
  const search = Object.entries(query)
    .filter(([, v]) => v !== undefined)
    .map(([k, v]) => `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`)
    .join('&');
  return search ? `${path}?${search}` : path;
}
```

**外部 API**

* 版本化：`/api/v1/...`（由 env 注入）
* 禁止在 URL 放敏感信息；长查询条件使用 POST body

---

## 6) 其他工程规范

**SFC 与组合式**

* 使用 `<script setup lang="ts">`；`defineProps/defineEmits` 写类型
* 少 props、多组合式；公共组件保持“受控”
* `v-for` 必须有稳定 `:key`（非索引）

**状态与副作用**

* 优先 `computed`；避免深度 `watch`；必要时 `watchEffect`

**错误与可观测性**

* 每个 `await` 明确错误处理；统一 `toast + 日志`
* 全局错误边界、上报携带版本/路由/权限快照

**样式**

* 设计令牌优先（CSS Vars/Tailwind 变量）
* SFC `<style scoped>` 或 CSS Modules（二选一、项目统一）

**网络层**

```ts
type ApiResp<T> = { code: number; msg: string; data: T }
const http = axios.create({ baseURL: import.meta.env.VITE_API_BASE });
```

* 统一超时/重试/取消；401/403/5xx 统一处理

**可访问性（A11y）**

* 交互元素可聚焦，提供可见焦点态
* 图片 `alt`，图标 `aria-label`；键盘可达性用例

**测试与门禁**

* 单测：Vitest；e2e：Playwright/Cypress
* 覆盖率：语句/分支/函数/行 ≥ 70%（核心 ≥ 85%）
* PR 门禁：类型检查 + ESLint + 测试 + 构建 全绿

**Git/版本/提交**

* 分支：`feat/users-bulk-import`、`fix/order-export-500`
* 提交（Conventional Commits）：`feat(users): support bulk import`
* 版本：SemVer + Changesets（自动生成变更日志）
* Pre-commit：`eslint --fix` + `prettier` + `tsc --noEmit` + `lint-staged`

---
